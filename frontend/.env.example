# MediConnect Frontend Environment Variables
# Copy this file to .env.local and update with your values

# Backend API Base URL (without /api suffix)
# Development (ngrok): https://your-ngrok-url.ngrok-free.dev
# Production: https://your-production-domain.com
# Local: http://localhost:5000

VITE_API_URL=https://shana-subcordate-raymon.ngrok-free.dev

# AWS Lambda API Gateway URL (for Login, Register, Upload Medical Record)
# Replace with your actual API Gateway URL after deployment
# Example: https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com/prod
VITE_LAMBDA_API_URL=https://ayvlc6aa4c.execute-api.us-east-1.amazonaws.com/prod

# Alternative: Individual Lambda Function URLs (if using Function URLs directly)
# Uncomment these if you prefer to use Lambda Function URLs instead of API Gateway
# VITE_LAMBDA_LOGIN_URL=https://xxxxxxxxxx.lambda-url.us-east-1.on.aws/
# VITE_LAMBDA_REGISTER_URL=https://xxxxxxxxxx.lambda-url.us-east-1.on.aws/
# VITE_LAMBDA_UPLOAD_URL=https://xxxxxxxxxx.lambda-url.us-east-1.on.aws/
