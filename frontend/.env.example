# MediConnect Frontend Environment Variables
# Copy this file to .env.local and update with your values

# AWS API Gateway Base URL (Lambda)
# All API calls (login, register, upload, appointments, etc.) go directly to API Gateway
# Example: https://ayvlc6aa4c.execute-api.us-east-1.amazonaws.com/prod
VITE_API_URL=https://ayvlc6aa4c.execute-api.us-east-1.amazonaws.com/prod

# Endpoints that will be used:
# POST /login (Lambda function)
# POST /register (Lambda function)
# POST /upload-medical-record (Lambda function)
# Other endpoints: /appointments, /doctors, /users, etc. (if available on API Gateway)
