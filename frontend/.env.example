# MediConnect Frontend Environment Variables
# Copy this file to .env.local and update with your values

# ============================================
# DUAL-BACKEND ARCHITECTURE
# ============================================

# Main Backend URL (ASP.NET Core on EC2/Express/ngrok)
# This handles ALL application endpoints with /api/* route prefixes:
# - /api/appointments, /api/doctors, /api/users
# - /api/medicalhistory, /api/medicalrecords
# - /api/profile, /api/admin/*, etc.
# Example: https://your-backend.ngrok-free.dev
VITE_API_URL=https://shana-subcordate-raymon.ngrok-free.dev

# AWS Lambda API Gateway URL (Auth & Upload ONLY)
# This handles ONLY these 3 endpoints (accessed directly, no /api prefix):
# - POST /login (LoginUser Lambda)
# - POST /register (RegisterUser Lambda)
# - POST /upload-medical-record (UploadMedicalRecord Lambda)
# Example: https://ayvlc6aa4c.execute-api.us-east-1.amazonaws.com/prod
VITE_LAMBDA_API_URL=https://ayvlc6aa4c.execute-api.us-east-1.amazonaws.com/prod

